// import axios from 'axios';

// // Create an Axios instance with default settings
// const api = axios.create({
//     baseURL: `${process.env.REACT_APP_BACKEND_URL}` || 'http://localhost:3000',  // Fallback to localhost if the env variable is not set
//     headers: {
//         'Content-Type': 'application/json',
//     },
// });

// export default api;
